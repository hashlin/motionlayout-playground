<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.linminphyo.motionlayoutplayground">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity
        android:name="co.linminphyo.motionlayoutplayground.MainActivity"
        android:theme="@style/AppTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity
        android:name=".CardStackActivity"
        android:theme="@style/AppTheme.FullScreen"/>
      <activity android:name=".DynamicCardStackActivity"/>
    <activity android:name=".ScaleImageActivity"/>
      <activity android:name=".DragWithDifferentInterpolator"/>
    <activity android:name=".ShapeMorphActivity"/>
    <activity android:name=".CreditCardActivity"/>
    <activity android:name=".LightingActivity"/>
    <activity android:name=".BubblesActivity"/>
    <activity android:name=".NavigationDrawerActivity"/>
    <activity android:name=".FlowActivity"/>
    <activity android:name=".recyclerview.RecyclerViewActivity"/>
  </application>

</manifest>